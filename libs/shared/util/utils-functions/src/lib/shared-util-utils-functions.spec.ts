import { sharedUtilUtilsFunctions } from './shared-util-utils-functions';

describe('sharedUtilUtilsFunctions', () => {
  it('should work', () => {
    expect(sharedUtilUtilsFunctions()).toEqual('shared-util-utils-functions');
  });
});
