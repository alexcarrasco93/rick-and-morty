import { rickAndMortySharedUtilModels } from './rick-and-morty-shared-util-models';

describe('rickAndMortySharedUtilModels', () => {
  it('should work', () => {
    expect(rickAndMortySharedUtilModels()).toEqual(
      'rick-and-morty-shared-util-models'
    );
  });
});
